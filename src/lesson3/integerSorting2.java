package lesson3;

import java.util.ArrayList;
import java.util.Arrays;

public class integerSorting2 {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
//  How to move zeros in the last - Output- {1, 2, 4, 3, 5, 0, 0, 0}
/*	int[] num = {1, 2, 0, 4, 3, 0, 5, 0};
    int nonZeroInd=0; // Position to place next non-zero element
//  Move non-zero elements to the front via swapping
	for (int i=0;i<num.length;i++) {
	if (num[i]!=0) {
//	if (num[i]==0) {   //Use == if want to move the zeros to front
//  Swap num[i] with num[nonZeroPos]
	int temp=num[i];
    num[i]=num[nonZeroInd];
	num[nonZeroInd]=temp;
	nonZeroInd++;
	}
	}
//  Optional: convert to ArrayList to match your code style
	ArrayList<Integer> arl = new ArrayList<>();
	for (int number:num) {
	arl.add(number);
	}
	System.out.println(arl);   */

//  For Practice
	

	}
}