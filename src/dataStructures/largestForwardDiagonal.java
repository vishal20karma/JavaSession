package dataStructures;

import java.util.HashMap;
import java.util.Scanner;

public class largestForwardDiagonal {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		
/*	Scanner sc = new Scanner(System.in);
//  Step 1: Read matrix dimensions
	int n = sc.nextInt();
	int m = sc.nextInt();
//  Step 2: Read matrix
	int[][] matrix = new int[n][m];
	for (int i = 0; i < n; i++)
	for (int j = 0; j < m; j++)
	matrix[i][j] = sc.nextInt();
	
//  Step 3: Use map to track diagonal sums
	
	HashMap<Integer, Integer> diagSums = new HashMap<>();
//  Step 4: Loop through the matrix
	for (int i = 0; i < n; i++) {
	for (int j = 0; j < m; j++) {
	int key = i + j; // Forward diagonal ID
	diagSums.put(key, diagSums.getOrDefault(key, 0) + matrix[i][j]);
	}
	}
	
//  Step 5: Find the maximum diagonal sum
	int maxSum = 0;
	for (int sum : diagSums.values()) {
	maxSum = Math.max(maxSum, sum);
	}
	
//  Step 6: Output the result
	System.out.println(maxSum);
	sc.close();   */
	
	}

}
